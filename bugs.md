# 问题
把“走到即将离开的尾巴位置”误判为死亡。

# 解决：
如果 new_head == food：
    这一步会吃食物，尾巴不会移动
    所以不能撞到任何身体部分

如果 new_head != food：
    这一步没吃食物，尾巴会移动走
    所以允许走到当前尾巴的位置


# 问题
每次训练会直接把上次训练的神经网络权重覆盖，无法保留多个权重，不利于复现以前的实验。

解决：修改文件夹结构，按照时间编号实验文件夹，不同的实验下有不同的权重和评估指标的记录。

